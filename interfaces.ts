export interface payload{
    id: string
}

export interface prestador{
    id: string;
    nome: string;
    email: string;
    senha: string;
    telefone: string;
    endereco: string;
    foto: string;
}