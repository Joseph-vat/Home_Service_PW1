export interface payload {
    id: string
}


export interface anuncio {
    titulo: string;
    descricao: string;
    preco: string
    servico: string;
    latitude: string;
    longitude: string;  
}